alias ll='ls -la'   \
      l1='ls -1'    \


# custome shortcuts
alias zt='zathura'

# package manager shortcuts
alias install='sudo pacman -S'
alias search='pacman -Ss'
alias update='sudo pacman -Syyu'
alias remove='sudo pacman -Runs'

# alias for easy editing and loading
alias ezh='nvim $HOME/.zshrc'
alias eza='nvim $HOME/.config/shell/aliasrc'
alias so='source $HOME/.zshrc'

# editing shotcuts
alias v='vim'
alias n='nvim'
alias em='emacs'
alias emn='emacs -nw'
alias ec='emacsclient -cnqu'
alias sec='sudo emacsclient -cnqu'

# exa shortcuts
alias es='exa --group-directories-first --icons'
alias ell='exa --group-directories-first --icons -l -a'
alias eil='exa --group-directories-first --icons -l -a -i'
alias ed='exa -D'

# helix
hx='helix'
